# Generate API_KEY with a password manager or:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
API_KEY=replace-with-a-long-random-value

# Generate a valid Fernet key with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DATA_ENCRYPTION_KEY=replace-with-a-fernet-key

BROWSER_DSN=http://browser:4444/wd/hub
HOST=0.0.0.0
LOG_LEVEL=INFO
ENVIRONMENT=production
DATA_DIR=/app/data

# Configure Telegram credentials only through the authenticated panel settings.
